// criando context para utilização, importar no "App" onde vou fazer a requisiçao
// e na "Table" onde vai ter a tabela
import { createContext } from 'react';

const Context = createContext();

export default Context;
