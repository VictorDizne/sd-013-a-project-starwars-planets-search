import React, { useState } from 'react';

function FilterByNumber({ value: { numberFilter, data } }) {
  const [test, setTest] = useState([]);

  return (
    <p>test</p>
  );
}

export default FilterByNumber;
